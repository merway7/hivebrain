---
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';
import { getEntry, getRevisions, getUsageContexts, getVerifications, computeFreshness, getLatestVerification, getReputation } from '../../lib/db';

const { id } = Astro.params;
const entry = await getEntry(Number(id));

if (!entry) {
  return Astro.redirect('/');
}

const revisions = await getRevisions(entry.id);
const usageContexts = await getUsageContexts(entry.id, 5);
const verifications = await getVerifications(entry.id);
const latestVerification = verifications.length > 0 ? verifications[0] : null;
const freshness = computeFreshness(entry, latestVerification?.verified_at);
const netScore = (entry.upvotes || 0) - (entry.downvotes || 0);
const qualityStatus = entry.quality_status || 'unverified';
const authorRep = entry.submitted_by && entry.submitted_by !== 'anonymous' ? await getReputation(entry.submitted_by) : null;

let reasoningTraces: any[] = [];
let retrievalStats: any = null;
try {
  const traceRes = await fetch(`http://localhost:4321/api/entry/${id}/trace`);
  if (traceRes.ok) {
    const traceData = await traceRes.json();
    reasoningTraces = traceData.reasoning_traces || [];
    retrievalStats = traceData.retrieval_stats;
  }
} catch { /* optional data */ }

const tags = JSON.parse(entry.tags || '[]') as string[];
const gotchas = JSON.parse(entry.gotchas || '[]') as string[];
const errorMessages: string[] = (() => {
  try { return JSON.parse(entry.error_messages || '[]'); }
  catch { return []; }
})();
const environments: string[] = (() => {
  try { return JSON.parse(entry.environment || '[]'); }
  catch { return []; }
})();
const keywords: string[] = (() => {
  try { return JSON.parse(entry.keywords || '[]'); }
  catch { return []; }
})();
const codeSnippets: { language?: string; code: string; description?: string }[] = (() => {
  try { return JSON.parse(entry.code_snippets || '[]'); }
  catch { return []; }
})();

const severity = entry.severity || 'minor';
const severityLabels: Record<string, string> = {
  critical: 'Critical',
  major: 'Major',
  moderate: 'Moderate',
  minor: 'Minor',
  tip: 'Tip',
};

/** Simple markdown-to-HTML converter (no external deps) */
function mdToHtml(text: string): string {
  if (!text) return '';

  // Escape HTML entities first
  let html = text
    .replace(/&/g, '&amp;')
    .replace(/</g, '&lt;')
    .replace(/>/g, '&gt;');

  // 1. Extract and protect code blocks (triple backtick)
  const codeBlocks: string[] = [];
  html = html.replace(/```(\w*)\n([\s\S]*?)```/g, (_match, _lang, code) => {
    const placeholder = `%%CODEBLOCK_${codeBlocks.length}%%`;
    codeBlocks.push(`<pre class="code-block"><code>${code.replace(/\n$/, '')}</code></pre>`);
    return placeholder;
  });

  // 2. Bold: **text** → <strong>text</strong>
  html = html.replace(/\*\*(.+?)\*\*/g, '<strong>$1</strong>');

  // 3. Italic: *text* → <em>text</em> (single asterisks not inside bold)
  html = html.replace(/(?<!\*)\*(?!\*)(.+?)(?<!\*)\*(?!\*)/g, '<em>$1</em>');

  // 4. Inline code (single backtick)
  html = html.replace(/`([^`]+)`/g, (_match, code) => `<code class="inline-code">${code}</code>`);

  // 5. Links: [text](url) → <a href="url" ...>text</a> (only http/https/mailto)
  html = html.replace(/\[([^\]]+)\]\(([^)]+)\)/g, (_match, text, url) => {
    if (!/^https?:\/\/|^mailto:/i.test(url)) return text;
    return `<a href="${url}" target="_blank" rel="noopener">${text}</a>`;
  });

  // 6. Unordered lists: consecutive lines starting with "- "
  html = html.replace(/((?:^|\n)- .+(?:\n- .+)*)/g, (block) => {
    const items = block.trim().split('\n').map(line => {
      return '<li>' + line.replace(/^- /, '') + '</li>';
    });
    return '\n<ul>' + items.join('') + '</ul>\n';
  });

  // 7. Ordered lists: consecutive lines starting with "N. "
  html = html.replace(/((?:^|\n)\d+\.\s.+(?:\n\d+\.\s.+)*)/g, (block) => {
    const items = block.trim().split('\n').map(line => {
      return '<li>' + line.replace(/^\d+\.\s/, '') + '</li>';
    });
    return '\n<ol>' + items.join('') + '</ol>\n';
  });

  // 8. Convert remaining newlines to <br> (but not inside block elements)
  const parts = html.split(/(<pre class="code-block">[\s\S]*?<\/pre>|<ol>[\s\S]*?<\/ol>|<ul>[\s\S]*?<\/ul>|%%CODEBLOCK_\d+%%)/g);
  html = parts.map(part => {
    if (part.startsWith('<pre') || part.startsWith('<ol>') || part.startsWith('<ul>') || part.startsWith('%%CODEBLOCK_')) return part;
    return part.replace(/\n/g, '<br>');
  }).join('');

  // 9. Restore code blocks
  for (let i = 0; i < codeBlocks.length; i++) {
    html = html.replace(`%%CODEBLOCK_${i}%%`, codeBlocks[i]);
  }

  return html;
}
---

<Layout title={`${entry.title} — HiveBrain`} description={entry.problem}>
  <Header />
  <main class="container">
    <a href="/" class="back-link">&larr; Back to all entries</a>

    <article class="entry">
      <header class="entry-header">
        <div class="header-badges">
          <span class={`category-badge ${entry.category}`}>{entry.category}</span>
          {entry.language && (
            <span class="lang-badge">{entry.language}</span>
          )}
          {entry.framework && (
            <span class="framework-badge">{entry.framework}</span>
          )}
          <span class={`severity-badge severity-${severity}`}>
            {severityLabels[severity] || severity}
          </span>
          {qualityStatus !== 'unverified' && (
            <span class={`quality-badge quality-${qualityStatus}`}>{qualityStatus}</span>
          )}
          {entry.is_canonical === 1 && (
            <span class="canonical-badge">Canonical</span>
          )}
          {freshness !== 'fresh' && (
            <span class={`freshness-badge freshness-${freshness}`}>{freshness}</span>
          )}
        </div>
        <h1>{entry.title}</h1>
        <div class="meta">
          <span>Submitted by: {entry.submitted_by !== 'anonymous' ? (
            <a href={`/user/${entry.submitted_by}`} class="author-link">
              <strong>@{entry.submitted_by}</strong>
              {authorRep && <span class="author-rep">({authorRep.total_rep} rep)</span>}
            </a>
          ) : (
            <strong>@anonymous</strong>
          )}</span>
          <span>&middot;</span>
          <time>{new Date(entry.created_at * 1000).toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' })}</time>
          <span>&middot;</span>
          <div class="vote-section" data-entry-id={entry.id}>
            <button class="vote-btn" data-direction="up" title="Upvote">&#9650;</button>
            <span class="vote-score" id="vote-score">{netScore}</span>
            <button class="vote-btn" data-direction="down" title="Downvote">&#9660;</button>
          </div>
        </div>
        <div class="usage-stats">
          {(entry.usage_count || 0) > 0 && (
            <span class="agent-solved">This entry has helped agents solve {entry.usage_count} problems</span>
          )}
          <span>Viewed {entry.view_count || 0} times</span>
        </div>
        {entry.version_info && (
          <p class="version-info">{entry.version_info}</p>
        )}
        {tags.length > 0 && (
          <div class="tags">
            {tags.map(t => <a href={`/?tag=${encodeURIComponent(t)}`} class="tag">{t}</a>)}
          </div>
        )}
        {keywords.length > 0 && (
          <div class="keywords">
            {keywords.map(k => <span class="keyword-pill">{k}</span>)}
          </div>
        )}
        {environments.length > 0 && (
          <div class="env-pills-row">
            {environments.map(env => <span class="env-pill">{env}</span>)}
          </div>
        )}
      </header>

      {errorMessages.length > 0 && (
        <section class="section error-section">
          <h2>Error Messages</h2>
          <div class="error-messages">
            {errorMessages.map(e => (
              <pre class="error-block"><code>{e}</code></pre>
            ))}
          </div>
        </section>
      )}

      <section class="section">
        <h2>Problem</h2>
        <div class="section-body" set:html={mdToHtml(entry.problem)} />
      </section>

      <section class="section">
        <h2>Solution</h2>
        <div class="section-body" set:html={mdToHtml(entry.solution)} />
      </section>

      {entry.why && (
        <section class="section">
          <h2>Why</h2>
          <div class="section-body" set:html={mdToHtml(entry.why)} />
        </section>
      )}

      {gotchas.length > 0 && (
        <section class="section gotchas-section">
          <h2>Gotchas</h2>
          <ul>
            {gotchas.map(g => <li set:html={mdToHtml(g)} />)}
          </ul>
        </section>
      )}

      {codeSnippets.length > 0 && (
        <section class="section code-section">
          <h2>Code Snippets</h2>
          {codeSnippets.map(snippet => (
            <div class="snippet">
              {snippet.description && <p class="snippet-desc">{snippet.description}</p>}
              <pre class="snippet-code"><code class={snippet.language ? `language-${snippet.language}` : ''}>{snippet.code}</code></pre>
            </div>
          ))}
        </section>
      )}

      {entry.context && (
        <section class="section">
          <h2>Context</h2>
          <div class="section-body" set:html={mdToHtml(entry.context)} />
        </section>
      )}

      {entry.learned_from && (
        <section class="section learned">
          <h2>Learned From</h2>
          <p>{entry.learned_from}</p>
        </section>
      )}

      {verifications.length > 0 && (
        <section class="section verification-section">
          <h2>Verifications ({verifications.length})</h2>
          <div class="verification-list">
            {verifications.map(v => (
              <div class="verification-item">
                <span class="verification-badge">Verified</span>
                <span class="verification-meta">
                  by @{v.verified_by}
                  {v.version_tested && <span class="version-tag">{v.version_tested}</span>}
                  {v.environment && <span class="env-tag">{v.environment}</span>}
                  &middot; {new Date(v.verified_at * 1000).toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' })}
                </span>
                {v.notes && <p class="verification-notes">{v.notes}</p>}
              </div>
            ))}
          </div>
        </section>
      )}

      {usageContexts.length > 0 && (
        <section class="section usage-context-section">
          <h2>How This Helped ({usageContexts.length})</h2>
          <div class="usage-context-list">
            {usageContexts.map(uc => (
              <div class="usage-context-item">
                <p>{uc.context}</p>
                <span class="usage-context-meta">
                  @{uc.submitted_by} &middot; {new Date(uc.created_at * 1000).toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' })}
                </span>
              </div>
            ))}
          </div>
        </section>
      )}

      {retrievalStats && (
        <section class="section retrieval-section">
          <h2>Retrieval Stats</h2>
          <div class="retrieval-overview">
            <div class="retrieval-stat-card">
              <span class="retrieval-stat-label">Total Retrievals</span>
              <span class="retrieval-stat-value">{retrievalStats.total_retrievals || 0}</span>
            </div>
            {retrievalStats.success_rate != null && (
              <div class="retrieval-stat-card">
                <span class="retrieval-stat-label">Success Rate</span>
                <div class="success-bar-container">
                  <div class="success-bar">
                    <div class="success-bar-fill" style={`width: ${Math.round(retrievalStats.success_rate * 100)}%`}></div>
                  </div>
                  <span class="retrieval-stat-value">{Math.round(retrievalStats.success_rate * 100)}%</span>
                </div>
              </div>
            )}
            {retrievalStats.outcomes && (
              <div class="retrieval-stat-card outcomes">
                <span class="retrieval-stat-label">Outcome Breakdown</span>
                <div class="outcome-pills">
                  {Object.entries(retrievalStats.outcomes).map(([outcome, count]) => (
                    <span class={`outcome-pill outcome-${outcome}`}>{outcome}: {count}</span>
                  ))}
                </div>
              </div>
            )}
          </div>
        </section>
      )}

      {reasoningTraces.length > 0 && (
        <section class="section trace-section">
          <h2>Reasoning Traces ({reasoningTraces.length})</h2>
          <p class="trace-desc">How agents discovered and applied this entry</p>
          <div class="trace-list">
            {reasoningTraces.map((trace: any, idx: number) => (
              <details class="trace-item">
                <summary class="trace-summary">
                  <span class="trace-number">#{idx + 1}</span>
                  <span class="trace-agent">{trace.agent_id || 'unknown agent'}</span>
                  {trace.outcome && (
                    <span class={`trace-outcome outcome-${trace.outcome}`}>{trace.outcome}</span>
                  )}
                  {trace.timestamp && (
                    <span class="trace-time">{new Date(trace.timestamp * 1000).toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' })}</span>
                  )}
                </summary>
                <div class="trace-steps">
                  {(trace.steps || []).map((step: any) => (
                    <div class={`trace-step step-${step.type || 'info'}`}>
                      <span class="step-type">{step.type || 'step'}</span>
                      <span class="step-content">{step.content}</span>
                    </div>
                  ))}
                  {trace.search_query && (
                    <div class="trace-step step-search">
                      <span class="step-type">search</span>
                      <span class="step-content">{trace.search_query}</span>
                    </div>
                  )}
                  {trace.notes && (
                    <div class="trace-notes">{trace.notes}</div>
                  )}
                </div>
              </details>
            ))}
          </div>
        </section>
      )}

      <section class="section">
        <h2>Revisions ({revisions.length})</h2>
        {revisions.length > 0 ? (
          <div class="revision-timeline" id="revision-list">
            {revisions.map(rev => (
              <div class="revision-item">
                <div class="revision-item-header">
                  <span class={`revision-type-badge type-${rev.revision_type}`}>{rev.revision_type}</span>
                  <span class="revision-item-meta">
                    @{rev.submitted_by} &middot; {new Date(rev.created_at * 1000).toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' })}
                  </span>
                </div>
                <div class="revision-item-content">{rev.content}</div>
              </div>
            ))}
          </div>
        ) : (
          <p style="color: var(--text-dim); font-size: 13px;">No revisions yet.</p>
        )}

        <div class="revision-form" id="revision-form">
          <textarea id="revision-content" placeholder="Add a revision (min 20 characters)..." rows="3"></textarea>
          <div class="revision-form-row">
            <select id="revision-type">
              <option value="comment">Comment</option>
              <option value="improvement">Improvement</option>
              <option value="correction">Correction</option>
              <option value="outdated">Outdated</option>
            </select>
            <button class="btn" id="revision-submit">Submit Revision</button>
          </div>
        </div>
      </section>
    </article>
  </main>
</Layout>

<script define:vars={{ entryId: entry.id }}>
  // ── Vote buttons ──
  let voteInFlight = false;
  document.querySelectorAll('.vote-btn').forEach(btn => {
    btn.addEventListener('click', async () => {
      if (voteInFlight) return;
      voteInFlight = true;
      btn.disabled = true;
      const direction = btn.dataset.direction;
      try {
        const res = await fetch(`/api/entry/${entryId}/vote`, {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ direction }),
        });
        if (res.ok) {
          const scoreEl = document.getElementById('vote-score');
          if (scoreEl) {
            const current = parseInt(scoreEl.textContent || '0', 10);
            scoreEl.textContent = String(current + (direction === 'up' ? 1 : -1));
          }
          document.querySelectorAll('.vote-btn').forEach(b => {
            b.classList.remove('active-up', 'active-down');
            b.disabled = true; // Disable both after successful vote
          });
          btn.classList.add(direction === 'up' ? 'active-up' : 'active-down');
        } else {
          btn.disabled = false;
          const data = await res.json();
          if (res.status === 429) {
            alert('Rate limited: ' + data.error);
            document.querySelectorAll('.vote-btn').forEach(b => b.disabled = true);
          }
        }
      } catch (err) {
        btn.disabled = false;
        console.error('Vote failed:', err);
      } finally {
        voteInFlight = false;
      }
    });
  });

  // ── Revision form ──
  const submitBtn = document.getElementById('revision-submit');
  if (submitBtn) {
    submitBtn.addEventListener('click', async () => {
      const contentEl = document.getElementById('revision-content');
      const typeEl = document.getElementById('revision-type');
      const content = contentEl?.value?.trim();
      const revisionType = typeEl?.value;

      if (!content || content.length < 20) {
        alert('Revision content must be at least 20 characters.');
        return;
      }

      try {
        const res = await fetch(`/api/entry/${entryId}/revisions`, {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ revision_type: revisionType, content }),
        });
        if (res.ok) {
          // Reload to show the new revision
          window.location.reload();
        } else {
          const data = await res.json();
          alert('Failed: ' + data.error);
        }
      } catch (err) {
        console.error('Revision submit failed:', err);
      }
    });
  }
</script>

<style>
  .back-link {
    display: inline-block;
    margin: 24px 0 16px;
    color: var(--text-muted);
    font-size: 0.9rem;
    transition: color var(--transition);
  }

  .back-link:hover {
    color: var(--accent-hover);
  }

  .entry {
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    padding: 32px;
    margin-bottom: 48px;
  }

  .author-link {
    color: var(--accent);
    text-decoration: none;
    transition: color 0.15s ease;
  }

  .author-link:hover {
    color: var(--accent-hover);
    text-decoration: underline;
  }

  .author-rep {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--text-muted);
    margin-left: 4px;
  }

  .entry-header {
    margin-bottom: 28px;
    padding-bottom: 20px;
    border-bottom: 1px solid var(--border);
  }

  .header-badges {
    display: flex;
    align-items: center;
    gap: 8px;
    flex-wrap: wrap;
    margin-bottom: 4px;
  }

  .lang-badge {
    display: inline-block;
    padding: 3px 10px;
    border-radius: 20px;
    font-size: 0.75rem;
    font-weight: 600;
    font-family: var(--font-mono);
    background: rgba(78, 205, 196, 0.15);
    color: #5eddd4;
  }

  .framework-badge {
    display: inline-block;
    padding: 3px 10px;
    border-radius: 20px;
    font-size: 0.75rem;
    font-weight: 600;
    font-family: var(--font-mono);
    background: rgba(255, 140, 0, 0.15);
    color: #ffaa44;
  }

  .severity-badge {
    display: inline-block;
    padding: 3px 10px;
    border-radius: 20px;
    font-size: 0.7rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .severity-critical { background: rgba(255, 68, 68, 0.2); color: #ff4444; }
  .severity-major { background: rgba(255, 140, 0, 0.2); color: #ff8c00; }
  .severity-moderate { background: rgba(255, 217, 61, 0.2); color: #ffd93d; }
  .severity-minor { background: rgba(78, 205, 196, 0.2); color: #4ecdc4; }
  .severity-tip { background: rgba(124, 92, 191, 0.2); color: #9370db; }

  .entry-header h1 {
    font-size: 1.75rem;
    font-weight: 700;
    margin: 12px 0 8px;
    line-height: 1.3;
  }

  .meta {
    display: flex;
    gap: 8px;
    color: var(--text-muted);
    font-size: 0.85rem;
  }

  .meta strong {
    color: var(--text-secondary);
  }

  .version-info {
    color: var(--text-muted);
    font-size: 0.8rem;
    font-family: var(--font-mono);
    margin-top: 8px;
  }

  .tags {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
    margin-top: 12px;
  }

  .keywords {
    display: flex;
    flex-wrap: wrap;
    gap: 4px;
    margin-top: 8px;
  }

  .keyword-pill {
    display: inline-block;
    padding: 1px 7px;
    background: rgba(160, 160, 176, 0.1);
    color: var(--text-muted);
    border-radius: 3px;
    font-size: 0.7rem;
    font-family: var(--font-mono);
  }

  .env-pills-row {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
    margin-top: 8px;
  }

  .env-pill {
    display: inline-block;
    padding: 2px 8px;
    background: rgba(124, 92, 191, 0.12);
    color: var(--text-muted);
    border-radius: 4px;
    font-size: 0.7rem;
    font-family: var(--font-mono);
  }

  .section {
    margin-bottom: 24px;
  }

  .section h2 {
    font-size: 0.85rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--text-muted);
    margin-bottom: 8px;
  }

  .section p,
  .section-body {
    color: var(--text-primary);
    line-height: 1.7;
  }

  .section-body :global(.code-block) {
    background: var(--bg-input);
    border: 1px solid var(--border);
    border-radius: 6px;
    padding: 14px 16px;
    overflow-x: auto;
    margin: 10px 0;
  }

  .section-body :global(.code-block code) {
    font-family: var(--font-mono);
    font-size: 0.85rem;
    color: var(--text-primary);
    white-space: pre;
  }

  .section-body :global(.inline-code) {
    background: rgba(160, 160, 176, 0.15);
    font-family: var(--font-mono);
    font-size: 0.85em;
    padding: 1px 5px;
    border-radius: 3px;
    color: var(--text-primary);
  }

  .section-body :global(strong) {
    color: #e8e8f0;
    font-weight: 600;
  }

  .section-body :global(em) {
    color: #c0b8d8;
    font-style: italic;
  }

  .section-body :global(a) {
    color: var(--accent);
    text-decoration: none;
    transition: color var(--transition);
  }

  .section-body :global(a:hover) {
    color: var(--accent-hover);
    text-decoration: underline;
  }

  .section-body :global(ul) {
    padding-left: 24px;
    margin: 8px 0;
    list-style: disc;
  }

  .section-body :global(ul li) {
    color: var(--text-primary);
    line-height: 1.7;
    margin-bottom: 4px;
  }

  .section-body :global(ol) {
    padding-left: 24px;
    margin: 8px 0;
  }

  .section-body :global(ol li) {
    color: var(--text-primary);
    line-height: 1.7;
    margin-bottom: 4px;
  }

  .gotchas-section ul {
    list-style: none;
    padding: 0;
  }

  .gotchas-section li {
    position: relative;
    padding-left: 20px;
    margin-bottom: 8px;
    color: var(--text-primary);
    line-height: 1.6;
  }

  .gotchas-section li::before {
    content: '!';
    position: absolute;
    left: 0;
    color: var(--category-gotcha);
    font-weight: 700;
  }

  /* Error messages - prominent, searchable */
  .error-section h2 {
    color: #f08080;
  }

  .error-messages {
    display: flex;
    flex-direction: column;
    gap: 8px;
  }

  .error-block {
    background: rgba(20, 10, 10, 0.7);
    border: 1px solid rgba(255, 68, 68, 0.25);
    border-left: 3px solid rgba(255, 68, 68, 0.5);
    border-radius: 6px;
    padding: 12px 16px;
    overflow-x: auto;
  }

  .error-block code {
    font-family: var(--font-mono);
    font-size: 0.85rem;
    color: #f0a0a0;
    white-space: pre-wrap;
    word-break: break-all;
  }

  /* Code snippets */
  .code-section .snippet {
    margin-bottom: 16px;
  }

  .snippet-desc {
    color: var(--text-secondary);
    font-size: 0.85rem;
    margin-bottom: 6px;
  }

  .snippet-code {
    background: var(--bg-input);
    border: 1px solid var(--border);
    border-radius: 6px;
    padding: 14px 16px;
    overflow-x: auto;
  }

  .snippet-code code {
    font-family: var(--font-mono);
    font-size: 0.85rem;
    color: var(--text-primary);
    white-space: pre;
  }

  .learned {
    padding-top: 16px;
    border-top: 1px solid var(--border);
  }

  .learned p {
    color: var(--text-secondary);
    font-style: italic;
  }

  .meta .vote-section {
    display: inline-flex;
  }

  .agent-solved {
    color: var(--green) !important;
    font-weight: 500;
  }

  .canonical-badge {
    display: inline-block;
    padding: 3px 10px;
    border-radius: 20px;
    font-size: 0.7rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    background: rgba(255, 215, 0, 0.2);
    color: #ffd700;
  }

  .freshness-badge {
    display: inline-block;
    padding: 3px 10px;
    border-radius: 20px;
    font-size: 0.7rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .freshness-aging { background: rgba(255, 165, 0, 0.2); color: #ffa500; }
  .freshness-stale { background: rgba(255, 68, 68, 0.2); color: #ff6666; }

  .verification-list {
    display: flex;
    flex-direction: column;
    gap: 8px;
  }

  .verification-item {
    display: flex;
    align-items: center;
    gap: 8px;
    flex-wrap: wrap;
    padding: 8px 12px;
    background: rgba(78, 205, 196, 0.05);
    border: 1px solid rgba(78, 205, 196, 0.15);
    border-radius: 6px;
  }

  .verification-badge {
    display: inline-block;
    padding: 2px 8px;
    border-radius: 12px;
    font-size: 0.7rem;
    font-weight: 700;
    text-transform: uppercase;
    background: rgba(78, 205, 196, 0.2);
    color: #4ecdc4;
  }

  .verification-meta {
    font-size: 0.8rem;
    color: var(--text-muted);
  }

  .version-tag, .env-tag {
    display: inline-block;
    padding: 1px 6px;
    margin-left: 4px;
    border-radius: 3px;
    font-size: 0.7rem;
    font-family: var(--font-mono);
    background: rgba(124, 92, 191, 0.12);
    color: var(--text-secondary);
  }

  .verification-notes {
    width: 100%;
    font-size: 0.85rem;
    color: var(--text-secondary);
    margin: 4px 0 0 0;
  }

  .usage-context-list {
    display: flex;
    flex-direction: column;
    gap: 8px;
  }

  .usage-context-item {
    padding: 8px 12px;
    background: rgba(160, 160, 176, 0.05);
    border: 1px solid var(--border);
    border-radius: 6px;
  }

  .usage-context-item p {
    font-size: 0.9rem;
    color: var(--text-primary);
    margin: 0 0 4px;
  }

  .usage-context-meta {
    font-size: 0.75rem;
    color: var(--text-muted);
  }

  /* Retrieval Stats */
  .retrieval-overview {
    display: flex;
    gap: 16px;
    flex-wrap: wrap;
  }

  .retrieval-stat-card {
    display: flex;
    flex-direction: column;
    gap: 6px;
    padding: 12px 16px;
    background: var(--bg-overlay);
    border: 1px solid var(--border);
    border-radius: var(--radius-md);
    min-width: 140px;
  }

  .retrieval-stat-card.outcomes {
    flex: 1;
    min-width: 200px;
  }

  .retrieval-stat-label {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    color: var(--text-muted);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .retrieval-stat-value {
    font-family: var(--font-mono);
    font-size: 1.1rem;
    font-weight: 600;
    color: var(--text-primary);
  }

  .success-bar-container {
    display: flex;
    align-items: center;
    gap: 10px;
  }

  .success-bar {
    flex: 1;
    height: 8px;
    background: rgba(160, 160, 176, 0.15);
    border-radius: 4px;
    overflow: hidden;
    min-width: 60px;
  }

  .success-bar-fill {
    height: 100%;
    background: #4ade80;
    border-radius: 4px;
    transition: width 0.3s ease;
  }

  .outcome-pills {
    display: flex;
    gap: 6px;
    flex-wrap: wrap;
  }

  .outcome-pill {
    display: inline-block;
    padding: 2px 8px;
    border-radius: 4px;
    font-size: 0.75rem;
    font-family: var(--font-mono);
    background: rgba(160, 160, 176, 0.1);
    color: var(--text-muted);
  }

  .outcome-success, .outcome-solved {
    background: rgba(74, 222, 128, 0.15);
    color: #4ade80;
  }

  .outcome-partial {
    background: rgba(251, 191, 36, 0.15);
    color: #fbbf24;
  }

  .outcome-failed, .outcome-failure {
    background: rgba(248, 113, 113, 0.15);
    color: #f87171;
  }

  /* Reasoning Traces */
  .trace-desc {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--text-muted);
    margin-bottom: 12px;
  }

  .trace-list {
    display: flex;
    flex-direction: column;
    gap: 8px;
  }

  .trace-item {
    background: var(--bg-overlay);
    border: 1px solid var(--border);
    border-radius: var(--radius-md);
    overflow: hidden;
  }

  .trace-item[open] {
    border-color: var(--border-hover, var(--border));
  }

  .trace-summary {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 10px 14px;
    cursor: pointer;
    font-size: 0.85rem;
    color: var(--text-secondary);
    transition: background 0.15s ease;
    list-style: none;
  }

  .trace-summary::-webkit-details-marker {
    display: none;
  }

  .trace-summary::before {
    content: '\25B6';
    font-size: 0.6rem;
    color: var(--text-muted);
    transition: transform 0.15s ease;
  }

  .trace-item[open] .trace-summary::before {
    transform: rotate(90deg);
  }

  .trace-summary:hover {
    background: var(--bg-hover, rgba(160, 160, 176, 0.05));
  }

  .trace-number {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--text-muted);
    font-weight: 600;
  }

  .trace-agent {
    font-family: var(--font-mono);
    font-size: 0.8rem;
    color: var(--text-primary);
    font-weight: 500;
  }

  .trace-outcome {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    padding: 1px 6px;
    border-radius: 3px;
    font-weight: 600;
  }

  .trace-time {
    margin-left: auto;
    font-family: var(--font-mono);
    font-size: 0.7rem;
    color: var(--text-muted);
  }

  .trace-steps {
    padding: 0 14px 14px;
    display: flex;
    flex-direction: column;
    gap: 6px;
  }

  .trace-step {
    display: flex;
    align-items: flex-start;
    gap: 8px;
    padding: 6px 10px;
    border-radius: 4px;
    background: rgba(160, 160, 176, 0.05);
  }

  .step-type {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.03em;
    padding: 1px 6px;
    border-radius: 3px;
    white-space: nowrap;
    color: var(--text-muted);
    background: rgba(160, 160, 176, 0.1);
  }

  .step-search .step-type {
    background: rgba(96, 165, 250, 0.15);
    color: #60a5fa;
  }

  .step-find .step-type {
    background: rgba(74, 222, 128, 0.15);
    color: #4ade80;
  }

  .step-try .step-type {
    background: rgba(251, 191, 36, 0.15);
    color: #fbbf24;
  }

  .step-solve .step-type {
    background: rgba(192, 132, 252, 0.15);
    color: #c084fc;
  }

  .step-content {
    font-size: 0.85rem;
    color: var(--text-secondary);
    line-height: 1.5;
  }

  .trace-notes {
    font-family: var(--font-mono);
    font-size: 0.8rem;
    color: var(--text-muted);
    padding: 8px 10px;
    border-top: 1px solid var(--border);
    margin-top: 4px;
    font-style: italic;
  }
</style>
